package hexlet.code;
/*import hexlet.code.Validator;
import hexlet.code.Schemas.StringSchema;*/

public class App {

    //isValid() будет обобщать функционал разных схем по проверке входных значений, он будет располагаться в BaseSchema.
    //ты методом isValid(Object val) будешь проверять это значение val на валидность. так вот есть у нас required()
    // не был выставлен и пришло значение null или не того типа, то это значение считается валидным. Если же required()
    // был выставлен, оно потом по проверке отвалится

}
